# 블로그 테마 가이드

## 시각 방향

- 전체는 짙은 네이비와 블랙을 바탕으로 한 다크 사이버펑크 분위기를 사용한다.
- 주요 강조색은 cyan, purple, pink 네온 계열로 제한한다.
- 네온 glow는 링크, 경계, 강조 요소에 절제해 사용하며 읽기보다 눈에 띄는 효과가 되지 않게 한다.
- 본문과 보조 텍스트, 카드·패널 표면은 어두운 배경에서 충분한 명도 대비를 유지한다.

## 공통 레이아웃과 표면

- 모든 페이지는 일관된 어두운 배경, 반투명 또는 짙은 표면, 섬세한 네온 경계와 그림자를 사용한다.
- 헤더와 탐색 링크는 명확히 구분하고, GitHub 프로필 링크는 작은 버튼 형태로 유지한다.
- 게시글 카드, 상세 글, 오류 상태, 버튼, 선택 상자, 색상·범위 입력은 같은 색 토큰과 표면 체계를 공유한다.
- 링크와 버튼은 hover 상태를 제공하되 과도한 애니메이션은 피한다.
- 모든 키보드 조작 대상에는 cyan 계열의 뚜렷한 focus 표시를 제공한다.

## 반응형

- 좁은 화면에서도 콘텐츠를 가로로 넘치게 하지 않고, 여백과 글자 크기는 유동적으로 조절한다.
- 카드와 본문은 읽기 쉬운 최대 폭을 유지한다.
- Ghost Fibers 플레이그라운드 글만 넓은 화면에서 더 넓은 콘텐츠 폭을 사용할 수 있다.
- 플레이그라운드 설정 패널은 넓은 화면에서는 다열, 좁은 화면에서는 한 열로 바뀐다.

## Ghost Fibers 전역 배경

- 전역 Ghost Fibers는 플레이그라운드와 동일한 고충실도 네이티브 WebGL2 렌더러를 사용한다.
- 효과는 전체 뷰포트를 채우는 장식용 배경이며, 모든 콘텐츠 뒤에만 표시하고 포인터 입력을 가로채지 않는다.
- 캔버스는 뷰포트와 drawing buffer 크기를 정확히 따라 재조정하고, 문서가 보이지 않을 때는 렌더링을 멈춘다.
- `prefers-reduced-motion`을 존중해 움직임을 중지하고 정적 결과를 유지한다.
- WebGL2를 지원하지 않는 환경에서는 사이트 기능을 방해하지 않는 명시적 안내 상태를 표시한다.

## Ghost Fibers 플레이그라운드

- 플레이그라운드는 독립된 넓은 반응형 게시글 레이아웃을 사용한다.
- 상단에는 반응형 직사각형 미리보기 프레임을 두고, 효과 캔버스는 오버레이 텍스트 뒤의 비대화형 배경으로 배치한다.
- 모든 설정 컨트롤은 미리보기 아래에 두며, 기능별 그룹과 명확한 레이블을 제공한다.
- 슬라이더, 색상 선택, 토글, 선택 상자 등은 실제 렌더링 설정을 즉시 바꿔야 한다.
- 공식 Ghost Fibers 설정 범위와 기본값을 제공하고, 모든 값을 복원하는 Reset 버튼을 유지한다.
- 구현은 원본과 같은 시각적 의도를 따르되 이 사이트의 네온 테마와 접근성 기준에 맞춘다.

## 게시글 카드 스포트라이트

- 스포트라이트 효과는 게시글 목록의 카드에만 적용한다.
- 각 카드는 자신의 내부 포인터 위치를 따라가는 독립적인 cyan 네온 스포트라이트를 사용한다.
- 키보드 포커스 시에도 포인터 없이 동등한 강조 상태가 보여야 한다.
- 효과 레이어는 `pointer-events: none`이어야 하며, 카드 링크와 탐색 동작을 방해하지 않는다.
- `prefers-reduced-motion`에서는 추적과 시각 효과를 비활성화한다.

## 참고 사이트

- Ghost Fibers의 시각·동작 참고: [Ghost Fibers](https://reactbits.dev/backgrounds/ghost-fibers)
- 게시글 카드 스포트라이트의 시각·동작 참고: [Spotlight Card](https://reactbits.dev/components/spotlight-card)
- 다크 사이버펑크 네온 방향은 외부 참고가 아닌 이 프로젝트의 디자인 방향이다.
- 참고 링크는 시각과 동작의 의도를 위한 것이며, 상위 프로젝트의 브랜딩, 페이지 레이아웃, 에셋 또는 소스 코드를 복사하라는 지침이 아니다.

## 접근성 및 성능

- 의미 있는 HTML 구조와 연결된 레이블을 사용하고, 장식용 캔버스는 보조 기술에서 숨긴다.
- 읽기 쉬운 색 대비, 키보드 탐색, 명확한 오류 상태를 기본으로 한다.
- 애니메이션은 reduced motion, 문서 가시성, 화면 크기 변화를 고려한다.
- 카드·캔버스·이벤트 리스너는 페이지와 인스턴스 범위로 제한하고, 다른 페이지나 컴포넌트에 동작이 누출되지 않게 한다.
